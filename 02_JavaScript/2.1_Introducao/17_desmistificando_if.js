// Aula: Desmistificando o IF
// O if pode ser usado com qualquer expressão que resulte em true ou false

let valor = '';
if (valor) {
    console.log('Valor é verdadeiro');
} else {
    console.log('Valor é falso');
}
// Strings vazias, 0, null, undefined e NaN são considerados "falsos" em JS